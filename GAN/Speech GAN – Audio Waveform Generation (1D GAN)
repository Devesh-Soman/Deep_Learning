import numpy as np
from tensorflow.keras.models import Sequential
from tensorflow.keras.layers import Dense, Conv1DTranspose, Conv1D, Flatten

# Synthetic 1D waveforms
data = np.sin(np.linspace(0,100,2048)) + np.random.rand(2048)*0.1
data = data.reshape(1,-1,1)

# Generator
generator = Sequential([
    Dense(512,input_dim=100),
    Dense(2048,activation='tanh'),
    Reshape((2048,1))
])

# Discriminator
discriminator = Sequential([
    Flatten(input_shape=(2048,1)),
    Dense(256,activation='relu'),
    Dense(1,activation='sigmoid')
])
discriminator.compile(optimizer='adam',loss='binary_crossentropy',metrics=['accuracy'])
